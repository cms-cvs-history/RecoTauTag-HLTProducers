process SELECTOR = {

   include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
   include "Geometry/CaloEventSetup/data/CaloGeometry.cfi"	
   include "FWCore/MessageLogger/data/MessageLogger.cfi"
   untracked PSet maxEvents = { untracked int32 input = 100}  

   source = PoolSource 
    {
         untracked vstring fileNames =
         {     
		'file:/scratch/bachtis/test.root'
         }
    
    }

  include "RecoTauTag/HLTProducers/data/L2TauIsolationProducer.cfi"
  include "RecoTauTag/HLTProducers/data/L2TauIsolationSelector.cfi"


  module output = PoolOutputModule 
        {
                untracked string fileName = "testprod.root"
                untracked vstring outputCommands = 
                {
                        "drop *",
#                       "keep *_*_*_HLT",
                       "keep *_*_*_SELECTOR"
                }
        }
        path producer = {doubleTauL2Producer,doubleTauL2Selector}
	endpath end ={output}
}
